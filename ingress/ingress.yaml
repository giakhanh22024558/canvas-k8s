apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: canvas
  namespace: canvas
spec:
  rules:
    - host: canvas.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: canvas
                port:
                  number: 80